# Ansible Role: Install and configure fzf

Install and configure fzf. Configurtion works in airgapped environments.

## Example Playbook
```
- hosts: localhost
  roles:
    - { role: darthkoax.fzf.role }
```
